% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heat_unit_functions.R
\name{stress_degree_unit}
\alias{stress_degree_unit}
\title{Calculate Daily Stress Degree Units (SDU)}
\usage{
stress_degree_unit(temp, lower_opt = 10, upper_opt = 30)
}
\arguments{
\item{temp}{A numeric vector of daily mean temperatures (in degrees Celsius).}

\item{lower_opt}{A numeric value defining the lower optimum temperature (default is 10°C).
Set to NULL to skip lower stress computation.}

\item{upper_opt}{A numeric value defining the upper optimum temperature (default is 30°C).}
}
\value{
A numeric vector of daily SDU values.
}
\description{
Computes the daily Stress Degree Unit (SDU) by measuring the deviation of the daily temperature
from an optimal range. If the temperature is below a lower optimum (when provided) or above an upper optimum,
the function returns the corresponding deviation. When the temperature falls within the optimal range,
no stress is accumulated (i.e., zero). If \code{lower_opt} is set to NULL, cold stress is not computed.
}
\examples{
# For a vector of daily temperatures with default lower_opt:
temps <- c(7, 15, 33)
stress_degree_unit(temps)

# For a vector of daily temperatures with no lower stress calculation:
stress_degree_unit(temps, lower_opt = NULL, upper_opt = 30)

}
