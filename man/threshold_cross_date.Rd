% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{threshold_cross_date}
\alias{threshold_cross_date}
\title{Identify Threshold Crossing Date for Cumulative Thermal Units}
\usage{
threshold_cross_date(
  data,
  cumulative_col,
  threshold,
  date_col = "time",
  group_vars = c("locationID", "Year")
)
}
\arguments{
\item{data}{A data frame that contains cumulative thermal unit data and a date column.}

\item{cumulative_col}{A string specifying the column name with cumulative values (e.g., "cumulative_daily_gdu").}

\item{threshold}{A numeric value representing the threshold level to be exceeded (e.g., 2000).}

\item{date_col}{A string specifying the name of the date column (default: "time").}

\item{group_vars}{A character vector of column names for grouping (default: c("locationID", "Year")).}
}
\value{
A data frame with one row per group that reports the first date the cumulative value exceeds the threshold.
}
\description{
Identifies, within groups (e.g., by location and year), the first date on which the cumulative
thermal unit value exceeds a given threshold. This is particularly useful for predicting phenological events.
}
\details{
The function filters for rows where the cumulative value meets or exceeds the threshold,
then selects the earliest date per group.
}
\examples{
df <- data.frame(
  locationID = rep("A", 5),
  Year = rep(2025, 5),
  time = as.Date('2025-04-01') + 0:4,
  cumulative_daily_gdu = c(0, 2, 7, 10, 15)
)
threshold_cross_date(df, cumulative_col = "cumulative_daily_gdu", threshold = 8)

}
